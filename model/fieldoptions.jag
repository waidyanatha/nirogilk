<%
function db_query_sel_fo(opt_category) {
    var outStr = "SELECT opt_category, opt_index, opt_name FROM field_options WHERE opt_category = '" + opt_category + "' LIMIT 0 , 30; ";
    log.info("MODEL : fieldoptions EXEC : db_query_fo_get("+opt_category+") RETURN : " + outStr);
    return outStr;
}
%>